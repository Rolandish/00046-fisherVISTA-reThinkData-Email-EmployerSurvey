{{!
	Rule
	Params:
		orientation: (string) accepts 'v', 'h'
		mobile: (string) accepts 'd', 'h'
}}
<t{{ drop }} class="{{ orientation }}-rule-{{ drop }}">
	<img alt="spacer" src="../dist/img/spacer.gif" />
</t{{ drop }}>
